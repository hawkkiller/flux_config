apiVersion: v1
kind: ConfigMap
metadata:
  name: chat-gpt-value-overrides
  namespace: chat-gpt
data:
  values.yaml: |
    service:
      port: 3000
    ingress:
      enabled: true
      annotations:
          kubernetes.io/ingress.class: nginx
      hosts:
          - host: chat-gpt.example.com
            paths:
              - path: /(.*)
                pathType: Prefix
    resources:
      requests: 
        cpu: 10m
        memory: 100Mi
      limits:
        cpu: 100m
        memory: 500Mi
        